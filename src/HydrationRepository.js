class HydrationRepository {
  constructor(data){
    this.data = data
    }
  }
  if (typeof module !== 'undefined') {
    module.exports = HydrationRepository;
  } else {
    module.exports = HydrationRepository;
  }
